{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ona&nel — Web App",
            "type": "editor-browser",
            "request": "launch",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}/build/web"
        }
    ]
}